# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Deliveree' do
	use_frameworks!

	# Pods for Deliveree

	pod 'Kingfisher', '~> 5.4.0'  # Image downloader
	pod 'Moya', '~> 13.0.0'       # Network abstraction layer
	pod 'SnapKit', '~> 5.0.0'     # Programatic UI
  pod 'SwiftyJSON', '~> 5.0'    # JSON parsing

end
